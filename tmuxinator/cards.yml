# ~/.tmuxinator/cards.yml

name: cards
root: ~/Dev/7r/cards
windows:
  # Frontend
  - frontend:
      root: ~/Dev/7r/cards/frontend
      panes:
        - code . && yarn && yarn dev
  # Backend
  - backend:
      root: ~/Dev/7r/cards/backend
      panes:
        - code . && yarn && yarn dev
